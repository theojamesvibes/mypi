fastapi==0.139.0
# starlette is a transitive dep of fastapi (which only constrains it to
# >=0.46.0 with no ceiling). Pin an explicit security floor so a resolve
# can't land on a version exposed to CVE-2026-54283 (urlencoded form-size
# limits silently ignored → DoS, fixed in 1.3.1) or the earlier multipart/
# Range DoS issues (CVE-2025-54121 / -62727).
starlette>=1.3.1
uvicorn[standard]==0.50.0
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.5
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.2
pyyaml==6.0.3
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
jinja2==3.1.6
apscheduler==3.11.3
cryptography==49.0.0
slowapi==0.1.10
